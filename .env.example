# Example environment variables for the application
# Prefix any variables used in front-end code with VITE_ (required by Vite)
VITE_APP_NAME=eEmployeeID
# Example: VITE_API_URL=https://api.example.com (unused in this frontend-only project)

# Note: Copy this file to .env and set any secret values locally. Do NOT commit .env to git.